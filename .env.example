# CherryPi Environment Configuration
# Copy this file to .env and configure the values

# Authentication Enable/Disable
# Set to 'false' to disable authentication entirely (all endpoints become public)
# AUTH_ENABLED=true

# JWT Secret Key - Used to sign authentication tokens
# Generate a strong random key: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this

# Auth Database Encryption Key - Used to encrypt the user database at rest
# Generate a strong random key: python -c "import secrets; print(secrets.token_hex(32))"
AUTH_DB_KEY=your-super-secret-db-encryption-key-change-this

# Optional: CherryPi URL for Magic QR codes (used in QR code generation)
# CHERRYPI_URL=http://cherrypi.local:3000
